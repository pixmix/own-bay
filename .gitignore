# Data files (contain user content and credentials)
site/data/items.json
site/data/offers.json
site/data/settings.json
site/data/marketplace.db
site/data/marketplace.db-wal
site/data/marketplace.db-shm
site/data/*.migrated

# Uploaded images
site/uploads/*
!site/uploads/.gitkeep

# Built deployment packages
2h-deploy.sh
2h-deploy.tar.gz

# Source photos
photos/
*.zip

# Claude Code
.claude/

# Scratch tools cache
tools/scratch/

# IDE
.idea/
.vscode/
*.swp
*.swo
*.bak

# Coordination onboarding scratch (disposable)
_ONBOARDING-CURRENCY.md

# Obsidian workspace (machine-specific)
.obsidian/

# Production backup sets pulled by ftp-deploy.sh
prod-backup-*/

# Operator runbook + instance-specific deploy tooling — names production
# accounts and a co-tenant service; kept local, out of the public repo.
Deploy SOP.md
ftp-deploy.sh

# Project contacts (mesh standard, n-447) — names counterparties and a
# co-tenant service; same reasoning as the Deploy SOP, kept local.
Contacts.md
